import React from "react";
const NoResultsMessage = (props) => {

  return (
    <p>
      There are no results for your search. Please change the filters to adjust your search.
    </p>
  )

};

export default NoResultsMessage;
