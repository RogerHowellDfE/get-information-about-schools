import React from "react";
import { useSelector } from 'react-redux';
const maxPages = 5;

const paginationStart = useSelector(state => {

});

const paginationFinish = useSelector(state => state.resultCount);



const GiasPagination = props => {

};

export default GiasPagination;
